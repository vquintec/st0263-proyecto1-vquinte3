<% include header %>

<script>
  function deleteVideo(id) {
    url = "http://10.131.137.188/vquinte3/delete-video/" + id;
    deleteRequest(url);
  }

  function deleteRequest(url) {
    $.ajax({
      url: url,
      type: 'DELETE',
      success: function(res) {
        console.log(res);
        alert(res);
        location.reload();
      },
      error: function(data){
        alert("fail " + data);

      }
    });
  }

</script>

<h1><%-title %></h1>

<a href="<%- baseUrl %>upload-video">Upload video</a>


<table border="1">
  <tr>
    <td>Titulo</td>
    <td>Descripción</td>
    <td>Categoria</td>
    <td>Acciones</td>
  </tr>
  <% videos.forEach(function (video) { %>
  <tr>
    <td><%- video.title %></td>
    <td><%- video.description %></td>
    <td><%- video.category %></td>
    <td>
      <button onclick="deleteVideo('<%- video._id %>')">Borrar</button>
      <a href="<%- baseUrl %>edit-video?id=<%- video._id %>">Editar</a>
    </td>
  </tr>
  <% }); %>
</table>


<% include footer %>
